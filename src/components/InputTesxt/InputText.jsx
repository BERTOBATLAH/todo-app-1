import React from "react";

const InputText = () => {
  return (
    <input
      type="text"
      placeholder="Type here"
      className="input w-full max-w-xs"
    />
  );
};

export default InputText;
